{
  "name": "quickbite",
  "version": "1.0.0",
  "description": "QuickBite",
  "scripts": {
    "start": "concurrently --names \"api,web,queue,reverb,logs,scheduler\" --prefix-colors \"red,blue,green,yellow,white,magenta\" \"npm run backend\" \"npm run frontend\" \"npm run queue\" \"npm run reverb\" \"npm run logs\" \"npm run scheduler\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && php artisan serve",
    "queue": "cd backend && php artisan queue:listen --tries=1",
    "scheduler": "cd backend && php artisan schedule:work",
    "reverb": "cd backend && php artisan reverb:start --debug",
    "logs": "cd backend && php artisan pail --timeout=0",
    "install-all": "cd frontend && npm install && cd ../backend && npm install && composer install"
  },
  "author": "Lorenzo Lucchesi",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
